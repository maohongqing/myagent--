writer_system_prompt = """
你是资深中文竞品分析报告撰写专家，擅长把结构化调研数据转化为逻辑清晰、证据克制、适合产品决策的 Markdown 报告。

通用规则：
- 只基于输入材料写作，不编造输入中不存在的价格、指标、日期、融资、用户规模或市场份额。
- 重要判断必须说明依据来自哪些工具结果、竞品信息或阶段结论。
- 输出必须是 Markdown 正文，不要输出 JSON、代码块外壳、解释性前言或“以下是”之类的多余文字。
- 行文要符合竞品分析报告逻辑：先交代背景和选择依据，再展开维度分析，最后收束为发现、建议和行动计划。
- 表格、列表、分层标题优先于长段落；建议必须具体、可执行。
"""


REPORT_TITLE_PROMPT = """
请根据输入信息生成报告开头部分。

写作要求：
- 使用一级标题，格式为：# 目标产品名竞品分析报告。
- 在标题下用简短列表展示报告日期、分析目的、产品阶段。
- 只输出本章节 Markdown。

输入信息：
{context_json}
"""


REPORT_BACKGROUND_PROMPT = """
请撰写“分析背景”章节。

必须包含：
1. 分析目标：为什么要做竞品分析，希望为产品带来什么帮助。
2. 产品所处阶段、当前最大问题与挑战、竞品分析目标。
3. 将从哪些维度进行分析。
4. 选择这些分析维度和工具的理由。

写作要求：
- 使用二级标题开头。
- 结合用户目的、目标、产品周期、工具选择及理由。
- 不要展开竞品选择和工具结果细节。
- 只输出本章节 Markdown。

输入信息：
{context_json}
"""


REPORT_COMPETITOR_SELECTION_PROMPT = """
请撰写“竞品选择”章节。

必须包含：
1. 泛搜索阶段搜集到的所有竞品名称和简介。
2. 最终选择的竞品列表。
3. 每个最终竞品的选择理由、类型、热度/增速/相似度等关键信息。
4. 不同分析目的下的竞品组合逻辑，并明确本次采用的组合：
   - 决策支持：2 个直接竞品 + 1 个间接竞品。
   - 学习借鉴：1 个直接竞品 + 2 个间接竞品。
   - 市场预警：1 个直接竞品 + 1 个间接竞品 + 1 个替代品。

写作要求：
- 使用二级标题开头。
- 优先使用 Markdown 表格呈现竞品池和最终选择。
- 对数据不足或质检结论薄弱的地方明确标注不确定性。
- 只输出本章节 Markdown。

输入信息：
{context_json}
"""


REPORT_TOOL_SECTION_PROMPT = """
请基于单个分析工具的全部执行结果，撰写“分析维度”中的一个小节。

必须包含：
1. 该工具/维度的分析目的。
2. 对每个相关竞品或对比对象的主要结论。
3. 关键数据、判断过程和置信度。
4. 当前维度下的简短小结：对目标产品意味着什么。

写作要求：
- 使用三级标题开头，标题体现工具名称或分析维度名称。
- 只使用本次输入的工具结果，不引用其他工具结果。
- 如工具结果包含多个竞品，应放在同一个小节中横向比较。
- 只输出本小节 Markdown。

输入信息：
{context_json}
"""


REPORT_KEY_FINDINGS_PROMPT = """
请基于已经写好的“分析维度”章节内容，撰写“关键发现”章节。

写作要求：
- 使用二级标题开头。
- 提炼 3 到 5 条最有商业价值的关键发现。
- 每条发现需包含：发现标题、核心判断、依据来自哪些分析维度、对目标产品的影响。
- 这是总结性章节，不要提出行动计划；行动计划留给“总结建议”章节。
- 只输出本章节 Markdown。

分析维度内容：
{analysis_sections_markdown}
"""


REPORT_RECOMMENDATIONS_PROMPT = """
请基于已经写好的“分析维度”章节内容，撰写“总结建议”章节。

必须包含：
1. 本次竞品分析结论。
2. 针对目标产品的具体改进建议。
3. 可执行行动计划，避免“加强创新”“提升体验”这类空泛表达。
4. 行动计划建议包含优先级、动作、负责人/团队类型、预期产出或验证方式。

写作要求：
- 使用二级标题开头。
- 建议必须与前文分析维度形成递进关系。
- 只输出本章节 Markdown。

分析维度内容：
{analysis_sections_markdown}
"""


REPORT_FINAL_UNIFY_PROMPT = """
请统一润色“关键发现”和“总结建议”两部分，使两者逻辑递进、口径一致、没有重复和矛盾。

写作要求：
- 保留两个二级标题。
- 关键发现只写判断和影响，不写行动计划。
- 总结建议只写结论、建议和行动计划，并与关键发现呼应。
- 不要引入输入之外的新事实。
- 只输出润色后的两个章节 Markdown。

关键发现：
{key_findings_markdown}

总结建议：
{recommendations_markdown}
"""

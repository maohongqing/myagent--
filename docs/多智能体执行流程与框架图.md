# 多智能体执行流程与框架图

本文档描述当前 `myagent` 后端实际运行的 Multi-Agent 工作流、状态树写入关系、前后端架构以及关键文件位置。当前代码入口为 `myagent/backend/app/agents/workflow.py`。

## 一、核心设计

当前后端以 `AgentWorkflow` 为主入口，主图中注册 6 个可路由节点：

- `plan_agent`：规划 Agent，负责阶段一需求理解、阶段三分析策略和采集策略规划。
- `search_agent`：搜索 Agent，负责阶段二候选竞品发现，以及阶段四触发资料卡采集。
- `analyst_agent`：分析 Agent，负责阶段二候选补充检索、热度/增速 QA、竞品评分、目标锁定，以及阶段四工具执行后的综合分析。
- `survey_design_agent`：问卷设计 Agent，可选节点，用于根据已锁定竞品生成用户调研问卷。
- `qa_agent`：质量检查 Agent，负责竞品阵型 QA 和最终状态树 QA。
- `writer_agent`：报告撰写 Agent，负责分章节生成最终 Markdown 报告。

还有两个重要的内部执行组件，不是 LangGraph 主图节点，但会在对应阶段被调用：

- `source_collector_agent`：由 `search_agent` 在 `workflow_phase == "phase_4_collect"` 时调用，负责资料卡 query、网页采集、chunk 填充、资料卡 QA 和缺口补搜。
- `tool_executor_agent`：由 `analyst_agent` 在 `workflow_phase == "phase_4_synthesis"` 时调用，负责执行已选分析工具并产出结构化 claims。

已移除：

- `structurer_agent` 已不再注册为主流程节点，`myagent/prompt/structurer_agent.py` 已删除。

中央状态树由 `create_initial_state_tree(request)` 初始化，核心节点包括：

- `phase_1_intent`：用户意图、生命周期、分析目的、目标产品、目标市场等。
- `phase_2_targets`：最终锁定的竞品集合、选择逻辑和候选评分结果。
- `phase_3_strategy`：分析框架、分析工具、采集维度和采集计划。
- `raw_data_sandbox`：阶段四采集得到的原始资料和证据摘要。
- `phase_4_synthesis`：综合分析结果、工具分析摘要和行动建议。
- `quality_gates`：checkpoint 1 和 checkpoint 2 的 QA 结果。
- `final_report_markdown`：最终 Markdown 报告正文。

## 二、当前执行流程图

```mermaid
flowchart TD
    A[用户提交竞品分析任务] --> B[plan_agent 阶段一：需求理解与澄清]

    B --> C{信息是否足够}
    C -- 需要澄清 --> C1[返回澄清问题，等待用户补充]
    C1 --> B
    C -- 足够 --> D[写入 phase_1_intent 和 analysis_intent]

    D --> E[search_agent 阶段二：生成候选搜索 query]
    E --> F[myagent.search 收集网页来源]
    F --> G[candidate_selector 按网页 chunk 抽取候选竞品]
    G --> H[qa_agent 候选存在性与相关性 QA]
    H --> I[analyst_agent 生成热度/增速补充 query]
    I --> J[补充搜索市场热度和增长信号]
    J --> K[qa_agent 热度/增速信息 QA]
    K --> L[analyst_agent 单竞品相关性评分]
    L --> M[锁定目标竞品并写入 phase_2_targets]

    M --> N[qa_agent Checkpoint 1：竞品阵型 QA]
    N --> O{Checkpoint 1 是否通过}
    O -- Reject --> L
    O -- Pass 且启用问卷 --> P[survey_design_agent 生成用户调研问卷]
    P --> Q[plan_agent 阶段三：分析策略与采集计划]
    O -- Pass 且不启用问卷 --> Q

    Q --> R[写入 phase_3_strategy、tool_plan、collection_plan]
    R --> S[search_agent 阶段四：进入资料卡采集]
    S --> T[source_collector_agent 资料卡 query、采集、填充、QA、补搜]
    T --> U[写入 raw_data_sandbox]

    U --> V[analyst_agent 阶段四综合]
    V --> W[tool_executor_agent 执行分析工具]
    W --> X[analyst_agent 汇总资料卡、工具结果和证据]
    X --> Y[写入 phase_4_synthesis]

    Y --> Z[qa_agent Checkpoint 2：全局一致性与防幻觉审查]
    Z --> ZA{Checkpoint 2 是否通过}
    ZA -- Reject --> V
    ZA -- Pass --> ZB[writer_agent 分章节生成最终报告]

    ZB --> ZC[写入 final_report_markdown]
    ZC --> ZD[保存 CompetitorReport 并返回前端]
```

## 三、Agent 与状态树读写关系

```mermaid
flowchart LR
    ST[(中央 JSON State Tree)]

    PA[plan_agent]
    SA[search_agent]
    SC[source_collector_agent<br/>内部组件]
    AA[analyst_agent]
    TE[tool_executor_agent<br/>内部组件]
    SV[survey_design_agent<br/>可选]
    QA[qa_agent]
    WA[writer_agent]

    PA -->|写入 phase_1_intent| ST
    PA -->|写入 phase_3_strategy| ST
    ST -->|读取 request / benchmark / candidates| PA

    SA -->|写入 candidates / candidate_sources| ST
    SA -->|阶段四触发采集| SC
    ST -->|读取 phase_1_intent / phase_3_strategy| SA

    SC -->|写入 raw_sources / evidence / detail_cards / report_references| ST
    SC -->|写入 raw_data_sandbox| ST

    AA -->|写入 competitor_brief_profiles / selected_competitor_set| ST
    AA -->|写入 analysis_report / phase_4_synthesis| ST
    AA -->|阶段四触发工具执行| TE
    ST -->|读取候选池 / 资料卡 / 工具结果 / strategy| AA

    TE -->|写入 tool_results / tool_sandboxes| ST

    SV -->|写入 survey_design| ST
    ST -->|读取目标竞品和分析基准| SV

    QA -->|写入 quality_gates / qa_history| ST
    ST -->|读取 phase_2_targets / phase_4_synthesis / complete state tree| QA

    WA -->|写入 final_report_markdown / report_chapters| ST
    ST -->|读取完整报告上下文| WA
```

## 四、后端工作流框架图

```mermaid
flowchart TB
    subgraph Frontend[前端 React + Vite]
        UI[用户界面]
        APIClient[src/api.ts]
        SSE[EventSource 实时事件流]
    end

    subgraph Backend[后端 FastAPI + LangGraph]
        Main[main.py<br/>FastAPI 路由]
        Workflow[AgentWorkflow<br/>工作流编排]
        LLM[llm.py<br/>OpenAI-compatible 模型适配]
        Storage[storage.py<br/>任务、事件、报告存储]
        Events[events.py<br/>事件总线]
    end

    subgraph GraphNodes[LangGraph 主节点]
        Plan[plan_agent]
        Search[search_agent]
        Analyst[analyst_agent]
        Survey[survey_design_agent]
        QAAgent[qa_agent]
        Writer[writer_agent]
    end

    subgraph InternalComponents[内部执行组件]
        SearchPkg[myagent.search<br/>多源搜索与网页证据]
        Collector[source_collector_agent<br/>资料卡采集]
        ToolExec[tool_executor_agent<br/>工具执行]
    end

    subgraph Prompts[提示词层 myagent/prompt]
        P1[plan_agent.py]
        P2[search_agent.py]
        P3[analyst_agent.py]
        P4[qa_agent.py]
        P5[card_agent.py]
        P6[tool_executor_agent.py]
        P7[survey_agent.py]
        P8[writer_agent.py]
        Bridge[prompt_bridge.py]
    end

    UI --> APIClient
    APIClient -->|/api/tasks| Main
    APIClient -->|/api/tasks/:id/events| SSE
    SSE --> Main

    Main --> Workflow
    Workflow --> Plan
    Workflow --> Search
    Workflow --> Analyst
    Workflow --> Survey
    Workflow --> QAAgent
    Workflow --> Writer

    Search --> SearchPkg
    Search --> Collector
    Analyst --> ToolExec

    Workflow --> Storage
    Workflow --> Events
    Events --> Main
    Main --> Storage

    Plan --> LLM
    Search --> LLM
    Analyst --> LLM
    Survey --> LLM
    QAAgent --> LLM
    Writer --> LLM
    Collector --> LLM
    ToolExec --> LLM

    Bridge --> P1
    Bridge --> P2
    Bridge --> P3
    Bridge --> P4
    Bridge --> P5
    Bridge --> P6
    Bridge --> P8
    Survey --> P7
    Workflow --> Bridge
```

## 五、QA 回路机制

```mermaid
flowchart TD
    A[qa_agent 接收状态树和 checkpoint] --> B{当前质检点}

    B -- checkpoint_1 --> C[检查 phase_1_intent 与 phase_2_targets]
    C --> D{竞品阵型是否匹配分析目的}
    D -- Pass --> E[进入 survey_design 或 phase_3_strategy]
    D -- Reject --> F[回到 analyst_agent 重新评分或定标]

    B -- checkpoint_2 --> G[检查完整状态树]
    G --> H{工具结果、资料卡和结论是否一致}
    H -- Pass --> I[进入 writer_agent]
    H -- Reject --> J[回到 analyst_agent 重新综合分析]

    F --> K{是否超过最大返工轮次}
    J --> K
    K -- 未超过 --> L[重新执行目标 Agent]
    K -- 已超过 --> M[放行但保留风险提示]
```

## 六、端口与调用关系

默认开发配置：

- 前端 Vite 端口：`5174`
- 后端 FastAPI 端口：`8000`
- 前端 `/api` 和 `/health` 请求由 Vite 代理到 `http://127.0.0.1:8000`
- 后端 CORS 建议包含 `http://localhost:5174` 和 `http://127.0.0.1:5174`

```mermaid
sequenceDiagram
    participant User as 用户
    participant FE as 前端 127.0.0.1:5174
    participant BE as 后端 127.0.0.1:8000
    participant WF as AgentWorkflow
    participant Search as myagent.search
    participant LLM as 模型网关

    User->>FE: 输入产品或竞品分析任务
    FE->>BE: POST /api/tasks
    BE->>WF: 创建并启动工作流
    WF->>Search: 候选搜索、资料采集、网页证据抽取
    WF->>LLM: 各 Agent 按阶段调用模型
    WF->>BE: 写入事件、状态树、报告
    FE->>BE: GET /api/tasks/{id}/events
    BE-->>FE: SSE 返回 Agent 进度
    FE->>BE: GET /api/tasks/{id}/report
    BE-->>FE: 返回 CompetitorReport
    FE-->>User: 渲染报告页面
```

## 七、关键文件

- `myagent/backend/app/agents/workflow.py`：工作流编排入口。
- `myagent/backend/app/agents/state_tree.py`：状态树初始化、合并和转换工具。
- `myagent/backend/app/agents/prompt_bridge.py`：后端到 prompt 文件的统一桥接层。
- `myagent/backend/app/agents/search_runner_adapter.py`：后端对 `myagent.search` 的适配层。
- `myagent/backend/app/agents/candidate_selector.py`：候选竞品 chunk 抽取、合并和筛选。
- `myagent/backend/app/agents/card_collector.py`：资料卡 query、采集、填充、QA 和补搜。
- `myagent/backend/app/agents/tool_executor.py`：分析工具执行。
- `myagent/backend/app/agents/survey.py`：问卷设计和问卷结果分析。
- `myagent/search/`：多源搜索、网页正文、截图、OCR、LLM 抽取和证据定位工具。
- `myagent/prompt/plan_agent.py`：规划 Agent prompt。
- `myagent/prompt/search_agent.py`：搜索 Agent prompt。
- `myagent/prompt/analyst_agent.py`：分析 Agent prompt。
- `myagent/prompt/qa_agent.py`：QA Agent prompt。
- `myagent/prompt/card_agent.py`：资料卡采集 prompt。
- `myagent/prompt/tool_executor_agent.py`：工具执行 prompt。
- `myagent/prompt/survey_agent.py`：问卷设计和问卷分析 prompt。
- `myagent/prompt/writer_agent.py`：报告撰写 prompt。
- `myagent/backend/app/main.py`：FastAPI 接口入口。
- `myagent/frontend/src/api.ts`：前端请求封装。
- `myagent/frontend/vite.config.ts`：前端端口和代理配置。

## 八、当前注意点

- `structurer_agent` 已删除，不应再出现在主流程图中。
- `source_collector_agent` 和 `tool_executor_agent` 是内部执行组件，不是当前 LangGraph 主图路由节点。
- `survey_design_agent` 是可选主图节点，仅在 `enable_survey_design_agent` 且尚未生成问卷时进入。
- `myagent.search` 已移动到 `myagent/search`，旧的根目录 `search/` 不再存在。
